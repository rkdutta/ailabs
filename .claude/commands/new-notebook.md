Create a new Jupyter notebook for the topic: $ARGUMENTS

1. Determine the appropriate directory (ml/supervised/, ml/unsupervised/, vector databases/, or a new one)
2. Create the notebook with:
   - A title and overview markdown cell
   - Import cells for relevant libraries (numpy, pandas, sklearn, matplotlib as needed)
   - Structured sections with markdown headers
   - Code cells with inline comments
3. Follow the existing notebook patterns in this repo
